[package]
name = "tson-rust"
edition = "2024"
description = "TSON (Typed JSON) parser implementation in Rust"
license = "MIT"

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

